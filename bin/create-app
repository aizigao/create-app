#!/usr/bin/env node

// enables us to use import in the other files
// eslint-disable-next-line no-global-assign
require = require('esm')(module /*, options */)

require('../src/cli').cli(process.argv.slice(2))
